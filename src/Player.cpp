#include "Player.h"

Player::Player(const std::string& name) : GameObject(name) {}

void Player::Start() {
  // Custom initialization for Player
}

void Player::Update() {
  // Custom update logic for Player
}
